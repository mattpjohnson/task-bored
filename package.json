{
  "name": "task-bored",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "^1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "update-demo": "cross-env PUBLIC_URL=https://rawgit.com/mattpjohnson/task-bored/master/docs npm run build && cp -R build/ docs/"
  },
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
